# 📐 内容创作与交付规范（CONTENT STANDARD）

> **唯一真相源（Single Source of Truth）**：本文件是「AI 深度解读 / 烨然漫笔」所有内容创作、审核、排版与交付的权威规范。
> 人机都能读：人类可据此理解品牌标准，智能体可据此直接执行并产出符合规范的内容。
> 所有历史散落在 `MEMORY.md`、`BRAND_GUIDELINES.md` 中的规范，均以本文件为准。

**适用对象**：公众号文章、个人网站全文、贴图/海报、小红书/朋友圈分享图。
**维护原则**：规范变更必须更新本文件，并在同一 commit 内同步引用它的文档（见 §0）。

---

## 目录

- [§0 文档同步铁律](#0-文档同步铁律)
- [§1 内容定位与文风](#1-内容定位与文风)
- [§2 写作规范（事实与措辞）](#2-写作规范事实与措辞)
- [§3 文件结构规范（四类交付物）](#3-文件结构规范四类交付物)
- [§4 公众号版规范](#4-公众号版规范)
- [§5 完整网页版规范](#5-完整网页版规范)
- [§6 贴图版规范（poster）](#6-贴图版规范poster)
- [§7 视觉设计规范](#7-视觉设计规范)
- [§8 素材生成规范](#8-素材生成规范)
- [§9 三轮审核规范](#9-三轮审核规范)
- [§10 发布流程 SOP](#10-发布流程-sop)
- [§11 命名与 Git 规范](#11-命名与-git-规范)
- [§12 术语与数据口径表](#12-术语与数据口径表)
- [§13 智能体执行检查清单](#13-智能体执行检查清单)

---

## §0 文档同步铁律

> **任何内容更新，必须在同一个 commit 内同步更新所有相关文档。**
> 不允许「先改内容、文档后补」——文档不同步 = 发布未完成。

| 更新类型 | 必须同步的文件 |
|---|---|
| 新增文章 | 文章目录（`README.md` + 四类文件）→ `articles/ai/README.md` 表格 → `articles/ai/articles.json` → `articles/ai/index.html` → 根 `README.md` 文章表 |
| 文章发布（状态变更） | 文章目录 `README.md` 状态 → `articles/ai/README.md` → `articles/ai/articles.json` → 根 `README.md` |
| `share/` 站点增删 | `share/README.md` 子目录表 → 根 `README.md` share 表 + 目录树 |
| 目录结构调整 | 根 `README.md` 目录树 → `share/README.md` 职责表 → 本文件 + `MEMORY.md` 所有路径引用 → `articles/ai/README.md` |
| 规范变更 | 本文件 → `MEMORY.md` → 根 `README.md` 对应章节交叉引用 |

### 0.1 内容源与发布状态

- **网站原文是 canonical reference edition**：保留完整证据链、可点击来源、图表/方法附录、事实截至日期与更新记录。
- **公众号版是完整但更易读的适配版**：必须独立讲完核心观点，不以故意截断换取“阅读原文”点击；阅读原文应提供逐条信源、交互图表、更新记录或附录等真实增量。
- 新文章应优先维护机器可读母稿（Markdown/MDX 或结构化正文），再生成 `article-full.html`、`article-wechat.html` 与 `poster.html`。存量文章在完成迁移前，任何事实修订必须同步两个 HTML 版本。
- 发布状态必须拆分：`editorial_status`（编辑成熟度）、`website_status`（网站是否公开）、`wechat_status`（公众号状态）。不得再用一个 `status` 同时表达“内容完成、网站上线、公众号发布”。
- `wechat_status=published` 只能依据公众号后台真实记录确认；网站页面可访问不代表公众号已经发布。
- 微信公众号 API 自动化止于草稿箱；正式发布或定时发布必须由作者在后台人工确认。

**commit 前自检**：

```bash
git status                          # 1. 确认是一次完整同步，无遗漏
grep -rn "旧文件名或旧路径" --include="*.md" .   # 2. 无旧名残留
grep -n "待发布\|已完成\|已发布" README.md articles/ai/README.md   # 3. 状态标记一致
grep -n '"status"' articles/ai/articles.json
```

---

## §1 内容定位与文风

### 1.1 品牌定位

- **公众号**：烨然漫笔（微信搜索「AI 深度解读」）/ 作者署名「艾AI」
- **使命**：报道新闻背后的商业逻辑和技术趋势，让普通人也能了解 AI，**不制造焦虑**
- **受众**：AI 创业者、算法工程师、科技投资人、产品经理

### 1.2 文风铁律

| 原则 | 说明 |
|---|---|
| **朴实平实** | 语言平易，不堆术语；复杂逻辑讲给所有人听 |
| **不制造焦虑** | 不渲染「AI 取代人类」「泡沫破裂」式情绪 |
| **犀利不刻薄** | 观点可以有锋芒，但不对人、不对公司进行人身攻击 |
| **自然幽默** | 不刻意搞笑，但允许自然流露的幽默 |
| **不追逐热点** | 追趋势背后的真相，而非蹭热度 |

### 1.3 标题规范

- **有吸引力，但不标题党**：可以制造悬念，但正文必须兑现承诺
- **严谨优先于耸动**：数据性标题必须与正文口径一致（见 §2）

---

## §2 写作规范（事实与措辞）

### 2.1 数据口径铁律（红线）

| 原则 | 说明 | 反面案例 | 正确写法 |
|---|---|---|---|
| **实际营收 vs ARR 必须区分** | ARR（年化运行收入）≠ 全年实际营收 | "2025全年营收突破200亿" | "2025全年实际营收约131亿；年底ARR突破200亿" |
| **ARR/run-rate 必须标注** | 所有增速数据注明是 ARR 而非实际营收 | "从60亿→250亿" | "OpenAI ARR/run-rate 从约60亿→约250亿美元" |
| **预测语气必须软化** | 前瞻预测不能写死 | "预计下个月突破500亿" | "若当前增速延续，有望在下个月突破500亿" |
| **"等效"措辞需谨慎** | 未获官方确认不造「等效」概念 | "约22万张H100等效GPU" | "超过22万张NVIDIA GPU，含H100/H200/GB200等" |
| **币种/单位/时间口径显式标注** | 不得混用 | "收入150亿"（未标币种） | "收入150亿元人民币" |

### 2.2 标题与正文严谨性

| 场景 | 要求 | 示例 |
|---|---|---|
| **不"推翻猜想"** | 反证上界/下界 ≠ 完整解决 | ✅ "反证了 Erdős 关于上界的核心猜想" |
| **精确描述成果** | 定性突破 vs 定量突破要分清 | ✅ "AI 构造推翻了 Erdős n^(1+o(1)) 上界猜想" |
| **读者不误解** | 正文补充澄清边界 | ✅ 导语补："并非完全求解 U(n)，其精确渐近仍是开放问题" |

### 2.3 措辞情绪化检查单（替换表）

| 原措辞 | 问题 | 建议替换 |
|---|---|---|
| "资本收割" | 情绪化、有判断倾向 | "公开市场融资" |
| "史上最大" | 需确认口径 | "目标估值 X 万亿，如获批将为史上最大 IPO" |
| "碾压""吊打" | 不客观 | "增速是 X 的 N 倍" |
| "烧钱竞赛" | 情绪化 | 需加客观框架，如"基础设施投资周期" |

### 2.4 数据来源铁律

- 优先官方信源、一手财报、可靠财经媒体；**媒体报道 ≠ 公司确认**
- 文中引用数据必须有来源标注；找不到来源的一律视为「待核实」
- **严禁编造数据**；引用需标明出处

---

## §3 文件结构规范（四类交付物）

### 3.1 标准目录结构

```
articles/ai/YYYY-MM-DD_slug/
├── README.md                  # 选题元数据（状态、日期、类型、信源）——状态唯一权威
├── article-wechat.html        # ① 公众号版：内联样式，复制粘贴样式不变
├── article-full.html          # ② 完整版：网页阅读，移动端适配，可交互
├── poster.html                # ③ 贴图版：竖版长图 + 一句导语（小红书/朋友圈/公众号图片消息）
├── cover-assets.html          # 公众号后台素材（头图/缩略图/朋友圈分享图）
├── article-illustrations.html # 内文配图素材（手绘风格）
├── review-log.md              # ④ 审核记录：每轮审核与修改流水账（见 §9）
├── en-article.html            # （可选）英文版
├── prompts/                   # AI 生图 prompt（备查）
└── assets/                    # 参考资料、图片素材
```

### 3.2 四类核心文件职责

| # | 文件 | 职责 | 关键约束 |
|---|------|------|---------|
| ① | `article-wechat.html` | 公众号版正文，唯一目标是**复制进公众号编辑器样式不变** | 内联 style、HEX、table 布局、meta 检查（见 §4）；**公众号不支持非公众号文章的外部链接**——正文不放外链，参考文献以纯文本列于文末 |
| ② | `article-full.html` | 完整版，面向网页与移动端阅读 | 可充分发挥 HTML：Chart.js/SVG 图表、动画、交互；深色科技风；移动端适配（见 §5） |
| ③ | `poster.html` | 贴图版，文章浓缩为一张竖版长图（标题 + 一句导语 + 核心要点 + 署名） | 竖版长图（约 1080 宽，2x 导出 2160 宽）；`poster.html` 是排版源，截图导出 `poster.png` 交付（见 §6） |
| ④ | `review-log.md` | 审核记录，每轮审核与修改的忠实记录者 | 结构见 §9；随文章终身保留，定稿后不删除 |

> `cover-assets.html` 与 `article-illustrations.html` 为辅助素材（见 §8）；`README.md` 为选题与状态唯一权威来源。

---

## §4 公众号版规范

### 4.1 排版铁律（Anthropic 900b 标准）

**✅ 正确做法**：
- 每个内容块用独立 `<section>` 包裹
- 每个 `<section>` 有 `background-color`（白 `#ffffff` / 浅灰 `#f8fafc` 交替）
- 内边距 `padding: 28px 24px`（上下 28、左右 24）
- 分隔线用独立 `<section style="padding:0 24px"><p style="height:1px;..."></p></section>`
- meta 标签：`<meta charset="UTF-8">`（有横杠）
- viewport：`content="width=device-width, initial-scale=1.0"`（**无** `user-scalable=no`）

**❌ 错误做法**：
- 所有内容塞进一个 `<section>`（铺满页面，无层次感）
- 无背景色区分（全是白底，读者眼疲劳）
- meta 写错：`charset="UTF-8"` 少横杠
- viewport 写错：多写 `user-scalable=no`
- **深色背景结语**（`#0f172a`/`#111827` 等）——公众号正文应保持白/浅灰浅色体系

### 4.2 HTML 语法强制检查清单

生成/修改 `article-wechat.html` 后必须检查：

```bash
grep -n 'charset="UTF' articles/ai/*/article-wechat.html     # 输出 charset="UTF-8"（有横杠）
grep -n 'user-scalable' articles/ai/*/article-wechat.html     # 应无输出
grep -n '<meta name="viewport"' articles/ai/*/article-wechat.html  # 只有 width=device-width, initial-scale=1.0
```

### 4.3 CSS 安全规范（公众号兼容）

```
✅ 允许：
- 所有样式写在内联 style="" 里
- 使用 HEX 颜色（#111111、#10b981、#3b82f6 等）
- 使用 table 实现分栏（不用 flex/grid）
- 使用 border-radius、box-shadow、padding、margin

❌ 禁止（会被公众号编辑器过滤）：
- rgba() 颜色 → 全用 HEX
- linear-gradient() → 用 table + 背景色模拟
- -webkit-background-clip: text → 无法在公众号使用
- CSS Grid / Flexbox → 用 table 替代
- position: absolute/fixed → 谨慎使用
```

### 4.4 外部链接与参考来源

- **正文不放任何非公众号文章的外部链接**
- 参考文献以**纯文本**列于文末（来源名称 + 标题，如"The Information、Reuters、公司 Q2 财报"）

---

## §5 完整网页版规范

### 5.1 定位

面向网页与移动端阅读，可充分发挥 HTML 特性，深色科技风。

### 5.2 能力

- **图表**：Chart.js / SVG 实现交互图表
- **动画**：入场动画、滚动视差、微动效
- **交互**：hover、展开/折叠、tooltip
- **移动端适配**：正文容器最大宽度 720px 居中；卡片手机单列、桌面双列

### 5.3 配色基调

深色科技风（详见 §7 色板），正文 `#e8e8e8`，辅助 `#888888`，标题 `#ffffff`。

---

## §6 贴图版规范（poster）

### 6.1 定位

文章浓缩为一张竖版长图，共用小红书 / 朋友圈 / 公众号「图片消息」。

### 6.2 规格（参考 `fable5-poster.html` 模板）

| 项 | 规格 |
|---|---|
| 画幅 | 竖版长图，设计宽 1080px（2x DPR 截图导出约 2160px 宽 PNG） |
| 结构 | 顶部 tag → 主标题 → 一句导语 → 核心要点/数据卡（3-5 个）→ 结尾（日期 + 署名「烨然漫笔」） |
| 内容 | 只放标题、一句导语、几个核心数字/要点，**不搬正文长段落** |
| 署名 | 底部带「烨然漫笔」标识 + 日期 |
| 交付 | `poster.html` 为排版源，浏览器 2x DPR 截图导出同目录 `poster.png` 交付 |

---

## §7 视觉设计规范

> 融合 Vercel（极简留白）、Claude（柔和对话感）、Google Material（信息层级）、Linear（高对比科技感），适配公众号（白底）与网站（深色科技风）双端。

### 7.1 品牌色板

**主色**

| 色值 | 名称 | 用途 |
|------|------|------|
| `#0a0a0a` | 纯黑 | 标题文字、深色背景 |
| `#1a1a2e` | 深蓝黑 | 深色卡片背景 |
| `#ffffff` | 纯白 | 公众号背景、浅色文字 |
| `#f5f5f5` | 浅灰 | 公众号卡片背景、分割线 |

**强调色**

| 色值 | 名称 | 用途 |
|------|------|------|
| `#5ac8fa` | 科技蓝 | AI/技术强调、链接、数据高亮 |
| `#76b900` | 英伟达绿 | 算力/芯片强调 |
| `#f39c12` | 警示金 | 数据/成本/商业强调 |
| `#e74c3c` | 警示红 | 安全/风险强调 |
| `#d4a574` | 暖金 | IPO/商业/金融强调 |
| `#ff6a00` | 阿里橙 | 生态/平台强调 |

**文字色阶**

| 色值 | 用途 |
|------|------|
| `#111111` | 公众号正文标题 |
| `#333333` | 公众号正文 |
| `#666666` | 辅助文字、引用 |
| `#999999` | 注释、日期、来源 |
| `#e8e8e8` | 深色模式正文 |
| `#888888` | 深色模式辅助文字 |

### 7.2 状态色语义（看板/监测通用）

> 这是本品牌所有数据可视化的颜色语义，必须一致。

| 颜色 | 含义 |
|---|---|
| 🟢 绿 `#10b981`/`#76b900` | 良好 / 正向 |
| 🟡 黄 `#f39c12` | 警告 / 需关注 |
| 🔴 红 `#e74c3c`/`#ef4444` | 风险 / 需行动 |

### 7.3 字体系统

```
公众号：-apple-system, BlinkMacSystemFont, "PingFang SC", "Helvetica Neue", Arial, sans-serif
网站：  -apple-system, BlinkMacSystemFont, "PingFang SC", "SF Pro Display", "Helvetica Neue", Arial, sans-serif
代码：  "SF Mono", "Fira Code", "Consolas", monospace
```

**字号层级**（公众号 / 网站）：

| 层级 | 公众号 | 网站 | 用途 |
|------|--------|------|------|
| H1 | 24px/700 | 32px/700 | 文章标题 |
| H2 | 20px/600 | 26px/600 | 章节标题 |
| H3 | 18px/600 | 22px/600 | 小节标题 |
| 正文 | 16px/400 | 18px/400 | 正文段落 |
| 引用 | 15px/400 | 16px/400 | 引用块 |
| 注释 | 13px/400 | 14px/400 | 日期/来源/图注 |
| 数据 | 14px/600 | 16px/600 | 数据卡片 |

**行高**：正文 1.8 · 标题 1.4 · 引用 1.6 · 列表 1.8

### 7.4 排版组件

- **段落间距** 24px · 标题与正文 16px · 列表项 8px · 引用块上下 20px · 图片上下 20px
- **引用块**：白底版 `#f9f9f9` 背景 + 左边框 3px 强调色 + 内边距 16px 20px + 圆角 4px；深色版 `rgba(255,255,255,0.05)` 背景 + 圆角 6px
- **数据卡片**：强调色 10% 透明度渐变 + 内边距 20px + 圆角 8px + 数字 24px/700 + 标签 13px/400
- **标签/徽章**：内边距 2px 10px + 圆角 12px（胶囊）+ 字号 12px/500 + 强调色 15% 透明背景
- **分割线**：公众号 `1px solid #e8e8e8` 上下 24px；网站 `1px solid rgba(255,255,255,0.1)` 上下 32px

### 7.5 文章主题色分配

每篇按内容主题从强调色选取一个主色调：

| 主题 | 主色调 | 辅助色 |
|------|--------|--------|
| AI 模型/技术 | `#5ac8fa` 科技蓝 | `#3498db` |
| 芯片/算力 | `#76b900` 英伟达绿 | `#2ecc71` |
| 商业/融资 | `#d4a574` 暖金 | `#f39c12` |
| 安全/风险 | `#e74c3c` 警示红 | `#ff6b6b` |
| 生态/平台 | `#ff6a00` 阿里橙 | `#e67e22` |
| 数据/趋势 | `#f39c12` 警示金 | `#f1c40f` |
| 默认 | `#5ac8fa` 科技蓝 | `#888888` |

### 7.6 配图规范

- **内文配图**：16:9（推荐 1200×675），圆角 8px，风格手绘/插画 > 实拍 > 截图
- **数据图表**：极简、无网格线、色块为主、从色板选色、数据柱/饼图 4px 圆角
- **图标**：线性 2px 描边、圆角 2px、24×24（内文）/48×48（数据卡片）

---

## §8 素材生成规范

### 8.1 `cover-assets.html` 标准结构

4 个素材（浏览器打开后截图）：

| 素材 | 尺寸 | 用途 |
|---|---|---|
| 公众号头图 | 900×383 | 公众号文章头图 |
| 缩略图 | 200×200 | 历史消息列表缩略图 |
| 朋友圈分享图 | 400×400（1:1） | 朋友圈分享卡片 |
| 信息卡片 | 680×约300 | 内文贴图 |

### 8.2 `article-illustrations.html` 标准结构

每 PART 一张配图，手绘风格：

```css
.sketch-box {
  background: #fffff7;             /* 米黄色背景 */
  border: 2.5px solid #2d2d2d;   /* 深色实线边框 */
  border-radius: 18px;
  box-shadow: 6px 6px 0 rgba(0,0,0,0.07);  /* 手绘阴影 */
}
.sketch-box::after {              /* 虚线阴影（手绘关键） */
  border: 2px dashed #ccc;
  border-radius: 18px;
}
```

---

## §9 三轮审核规范

> **每篇文章发布前必须通过至少三轮审核，且连续三轮无事实/数据错误方可定稿。** 审核是发布流程的硬性关卡，不是可选项。

### 9.1 审核轮次与通过标准

| 轮次 | 重点 | 通过标准 |
|---|---|---|
| 第 1 轮 | 事实核查：日期、金额、公司关系、产品发布状态、引用来源 | 所有硬错误均有修改方案 |
| 第 2 轮 | 修订验证：确认第 1 轮已修正 + 数据口径复查（ARR/实际、币种、估值/融资） | 无新增事实错误 |
| 第 3 轮 | 终稿把关：全文通读、标题严谨、措辞情绪化、排版规范 | 零事实/数据错误，可定稿 |

- 审核记录统一写入 `review-log.md`（**单文件，按轮次追加**）
- 任一轮发现新硬错误，修正后重新计轮：需连续无错误直至满三轮
- 三轮通过后，`README.md` 状态改「定稿」，方可进入发布流程

### 9.2 `review-log.md` 结构（三合一）

每个轮次一个 `## 第 N 轮审核（日期）` 章节，含四段：

```markdown
## 第 1 轮审核（2026-08-17）

### 审核结论
一句话判断：✅ 通过 / ⚠️ 修改后可过 / ❌ 需大改；硬错误、风险措辞、待核实各几条。

### 问题清单
| # | 文中声明 | 严重度 | 问题 | 修改建议 |
|---|---------|--------|------|---------|
| 1 | ... | 硬错误 | ... | 可直接替换的文本 |

### 修改记录
- [已解决] #1：旧表述 → 新表述
- [待确认] #3：...

### 状态
已解决 N 处 / 待确认 M 处；下轮审核重点：...
```

### 9.3 审核方法

- 硬事实与观点分开核；优先核查日期、金额、估值/融资、ARR/实际营收、产品发布状态、公司关系
- 优先官方信源、一手财报、可靠财经媒体；**媒体报道 ≠ 公司确认**
- 引用数据必须有来源；找不到来源一律「待核实」
- 币种、单位、时间口径显式标注，不得混用

---

## §10 发布流程 SOP

### 完整流程

```bash
# 1. 写作完成，人工审核
#    → 检查数据口径、标题严谨、措辞情绪化（见 §2）

# 2. 三轮审核定稿（见 §9），review-log.md 记录完整

# 3. 生成素材（见 §8）
#    → cover-assets.html（公众号后台素材）
#    → poster.html（竖版长图）
#    → article-illustrations.html（内文配图）
#    → 浏览器打开截图使用

# 4. 更新 README
#    → articles/ai/README.md 表格新增一行，状态"📝 待发布"

# 5. 更新首页
#    → articles/ai/index.html 新增 <section class="topic-section">

# 6. 提交代码（显式 add，避免 -A 误提交）
cd <仓库根目录>
git add <具体文件...>
git commit -m "feat: 新文章 slug"
git push

# 7. 部署到 GitHub Pages（push 后自动构建）
#    → Pages 源为 main 分支根目录，push 即触发部署
#    → 自定义域名 CNAME = yeranyang.cn

# 8. 验证部署 + 更新状态
#    → 浏览器打开 https://yeranyang.cn/ 检查
#    → 分别更新 website_status 与 wechat_status，不再使用含义模糊的“已完成”
```

### 公众号草稿 MVP

草稿推送使用 `scripts/wechat-draft-mvp.py`。凭证从仓库外的安全文件读取，默认路径为 `~/.workbuddy/secrets/wechat_yeranmanbi.env`；禁止将 AppID、AppSecret、access token 写入仓库、文章或日志。

```bash
python3 scripts/wechat-draft-mvp.py \
  articles/ai/YYYY-MM-DD_slug \
  --title "公众号标题" \
  --digest "分享摘要" \
  --dry-run
```

确认预检通过后去掉 `--dry-run`，脚本只创建草稿并返回 `media_id`。相同内容默认禁止重复创建；正式发布仍在公众号后台人工操作。

### 关键检查点

| 阶段 | 检查项 | 方法 |
|---|---|---|
| 写作完成 | 数据口径（实际 vs ARR） | 人工逐句检查 |
| 写作完成 | 标题/正文严谨性 | 「推翻猜想」vs「反证上界」 |
| HTML 生成 | meta 标签语法 | grep charset / viewport |
| HTML 生成 | 格式是否 Anthropic 标准 | 复制进公众号编辑器测试 |
| 首页更新 | 入口卡片是否齐全 | 对比上一篇 |
| 部署前 | git status 是否干净 | `git status` |
| 部署后 | 首页是否可访问 | 浏览器打开 |

---

## §11 命名与 Git 规范

### 11.1 目录命名

| 元素 | 规则 | 示例 |
|------|------|------|
| 日期 | `YYYY-MM-DD` 创作日期 | `2026-04-24` |
| slug | 英文 kebab-case，2-5 词 | `deepseek-v4-deep-dive` |
| 组合 | `日期_slug` 下划线连接 | `2026-04-24_deepseek-v4-deep-dive` |

> ⚠️ 统一 `YYYY-MM-DD`（四位年份），不用 `YY-MM-DD`。

### 11.2 commit message 规范

```
格式：<type>: <subject>
- type: feat / fix / docs / style / refactor
- subject: 中文 50 字内

示例：
feat: 新增 AI 数学证明文章（26-05-23_ai-math-proof）
fix: 修正 OpenAI 营收口径（实际 vs ARR）
docs: 更新 README 文章状态（✅ 已完成）
```

### 11.3 git add 与敏感文件

- ✅ 显式 `git add path/to/file`
- ❌ 避免 `git add -A`（复杂状态可能误 staged）
- 🚨 **token/密钥/证书绝不放在仓库目录**，放仓库外（`***REMOVED***.ssh/`、`***REMOVED***.config/` 等）；如必须放，先加 `.gitignore`
- commit 前 `git status` 确认无敏感文件

---

## §12 术语与数据口径表

> 精确用语速查表，写作与审核时对照使用。

| 术语 | 标准写法 | 易错点 |
|---|---|---|
| 年化运行收入 | ARR / run-rate | 常被误当「全年实际营收」 |
| 全年实际营收 | 全年实际营收（明确标注） | 与 ARR 混用 |
| 月活用户 | 月活 / MAU（标注来源） | 别写「月活用户数」模糊 |
| 营业利润率 | 营业利润率 operating margin | 别误称「毛利率」 |
| 毛利率 | 毛利率 gross margin | 与营业利润率区分 |
| 融资估值 | 「目标估值 X」+「如获批准」 | 「史上最大」需限定口径 |
| 积压订单 | backlog | — |

---

## §13 智能体执行检查清单

> 智能体（Claude/Workbuddy/其他）生成或修改内容时，按此清单逐项自检后再交付。

**写作阶段**：
- [ ] 数据口径：ARR 与实际营收已区分并标注
- [ ] 币种/单位/时间口径已显式标注
- [ ] 预测语气已软化（"有望/若延续"）
- [ ] 标题严谨（无"推翻猜想"式过度表述）
- [ ] 措辞无情绪化（对照 §2.3 替换表）
- [ ] 所有数据有来源，无编造

**文件结构阶段**：
- [ ] 四类文件齐全：`article-wechat.html` / `article-full.html` / `poster.html` / `review-log.md`
- [ ] 状态在 `README.md` 中正确标注

**公众号版阶段**：
- [ ] 每块独立 `<section>`，白/灰交替背景
- [ ] `<meta charset="UTF-8">`（有横杠）
- [ ] viewport 无 `user-scalable=no`
- [ ] 无 rgba/linear-gradient/flex/grid（用 HEX/table）
- [ ] 正文无外链，参考来源纯文本列文末
- [ ] 无深色背景结语（保持白/浅灰体系）

**完整版阶段**：
- [ ] 深色科技风（`#0a0a0a` 系背景）
- [ ] 移动端适配（720px 容器、卡片响应式）
- [ ] 图表为真实数据，非伪造

**贴图版阶段**：
- [ ] 竖版 1080 宽
- [ ] 只放标题+一句导语+核心要点+署名
- [ ] 底部有「烨然漫笔」+ 日期

**提交阶段**：
- [ ] 显式 `git add`，未误提交敏感文件
- [ ] 首页 index/README/articles.json 同步更新
- [ ] commit message 符合 `<type>: <subject>`

---

*首次整理：2026-08-18 · 整合自 MEMORY.md、BRAND_GUIDELINES.md 及三轮审核实战经验*
*维护者：AI 深度解读主理人*
